<UserControl x:Class="_1RM.View.Editor.Forms.RdpAppFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Shawn.Utils.Wpf.Controls;assembly=Shawn.Utils.Wpf"
             xmlns:forms="clr-namespace:_1RM.View.Editor.Forms"
             xmlns:controls1="clr-namespace:_1RM.Controls"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:protocol="clr-namespace:_1RM.Model.Protocol"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance forms:RdpAppFormViewModel}"
             d:DesignHeight="1450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="RowHeader">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <!-- 连接信息卡片 (现在包含在HostView中) -->
        <ContentControl s:View.Model="{Binding HostViewModel}"></ContentControl>

        <!-- RDP应用程序认证设置 -->
        <StackPanel>
            <!-- 认证信息内容 -->
            <Border Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="认证信息"
                               Style="{DynamicResource RowHeader}"/>

                    <ContentControl Grid.Column="1"
                                    s:View.Model="{Binding CredentialViewModel}"
                                    Margin="15,0,0,0"/>
                </Grid>
            </Border>

            <!-- Separator -->
            <Border Height="1" Background="{DynamicResource PrimaryMidBrush}" Opacity="0.3" Margin="0,0,0,12"/>
        </StackPanel>

        <!-- RDP文件附加设置 -->
        <StackPanel>
            <!-- RDP文件设置 -->
            <Border Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="RDP文件设置"
                               Style="{DynamicResource RowHeader}"/>

                    <Grid Grid.Column="1" Margin="15,0,0,0">
                        <Border BorderThickness="1"
                                CornerRadius="4"
                                BorderBrush="{DynamicResource PrimaryMidBrush}"
                                Padding="8">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding ElementName=TextBoxRdpFileAdditionalSettings, Path=IsMouseOver}" Value="True"/>
                                                <Condition Binding="{Binding ElementName=TextBoxRdpFileAdditionalSettings, Path=TextArea.IsFocused}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentMidBrush}"/>
                                            <Setter Property="Opacity" Value="0.8"/>
                                        </MultiDataTrigger>
                                        <DataTrigger Binding="{Binding ElementName=TextBoxRdpFileAdditionalSettings, Path=TextArea.IsFocused}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource AccentMidBrush}"/>
                                            <Setter Property="Opacity" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <controls1:BindableAvalonEditor
                                               x:Name="TextBoxRdpFileAdditionalSettings"
                                               WordWrap="False"
                                               ShowLineNumbers="True"
                                               Text="{Binding New.RdpFileAdditionalSettings}"
                                               FontSize="12"
                                               Height="120"
                                               MinHeight="120">
                                <controls1:BindableAvalonEditor.Resources>
                                    <VisualBrush x:Key="HelpBrush" TileMode="None" Opacity="0.3" Stretch="None" AlignmentX="Left">
                                        <VisualBrush.Visual>
                                            <TextBlock FontStyle="Italic" FontSize="12">tip e.g. usbdevicestoredirect:s:-USB\\VID_XXXX\\XXXX</TextBlock>
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </controls1:BindableAvalonEditor.Resources>
                                <controls1:BindableAvalonEditor.Style>
                                    <Style TargetType="controls1:BindableAvalonEditor">
                                        <Setter Property="Foreground" Value="{DynamicResource BackgroundTextBrush}"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="{x:Null}">
                                                <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background" Value="{StaticResource HelpBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </controls1:BindableAvalonEditor.Style>
                            </controls1:BindableAvalonEditor>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </StackPanel>

        <!-- 备选认证 -->
        <ContentControl s:View.Model="{Binding AlternativeCredentialListViewModel}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding AlternativeCredentialListViewModel.Credentials.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>

    </StackPanel>
</UserControl>